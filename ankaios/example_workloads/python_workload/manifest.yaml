apiVersion: v0.1
workloads:
  python_workload:
    runtime: podman
    agent: agent_A
    controlInterfaceAccess:
      allowRules:
        - type: StateRule
          operation: ReadWrite
          filterMask:
            - "*"
    runtimeConfig: |
      image: localhost/example_python_workload:0.1