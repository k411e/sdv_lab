apiVersion: v0.1
workloads:
  rust_workload:
    runtime: podman
    agent: agent_A
    controlInterfaceAccess:
      allowRules:
        - type: StateRule
          operation: ReadWrite
          filterMask:
            - "*"
    runtimeConfig: |
      image: localhost/example_rust_workload:0.1