apiVersion: v0.1
workloads:
  cruise_control_app:
    runtime: podman
    agent: agent_A
    restartPolicy: NEVER
    runtimeConfig: |
      image: ghcr.io/eclipse-sdv-hackathon-chapter-three/sdv-lab/cruise-control-app:latest
      commandOptions: ["-e", "RUST_LOG=debug", "--net=host", "-e", "MQTT_BROKER_URI=mqtt://localhost"]
